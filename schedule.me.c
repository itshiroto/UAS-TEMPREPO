#include<stdio.h>
#include<stdlib.h>

int main() {
    // create a program that would simulate CPU scheduling
    // using the Shortest Job First (SJF) algorithm
    // and the process must be printed in "time - executed job" format
    // and the program must calculate the average turnaround time
    // and the average waiting time
    // 
    
}